<?xml version="1.0"?>
<odoo>
    <data>
        <!-- <record model="ir.rule" id="rule_id">
            <field name="name">Rule name</field>
            <field name="model_id" ref="model_name"/>
            <field name="domain_force">[('attribute', 'operator', value)]</field>
            <field name="groups" eval="groups_attribute"></field>
            <field name="perm_write" eval="False"/>
        </record> -->
    </data>
</odoo>
